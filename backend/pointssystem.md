Points System

Currently when users complete a course, they get 0 points at all, but in an effort to make this tool gamified, I would like users to gain points when they complete a lesson. I would like the amount of points they get to be 100 * (questions the user got right on the first try in the session / total questions in session). Points are seperated by library, so if a user completes a session in library A, then it only updates library A points for that user.

I would also like the amount of points the user has in the individual library to be displayed on the libraries leaderboard.
Also, Please modify the session exist message to include something about the user not gaining any points if they leave early.

Please modify my database schemas and provide me a migration file as well. I am using Supabase Postgres major version 15